<!DOCTYPE html>
<html>
<head>
</head>
<body>

  <header>
  </header>

  <main>
    <% if (game) { %>
      <h1>Game #<%= game.id %></h1>
      <p><strong>Moves:</strong> <%= game.moves %></p>

      <table>
        <% for (let i = 0; i < 3; i++) { %>
          <tr>
          <% for (let j = 0; j < 3; j++) { %>
            <td>
            <% let curSquare = game.moves.indexOf(i*3+j) %>
            <% if (curSquare != -1) { %>
              <% if (curSquare % 2 === 0) { %>
                X
              <% } else { %>
                O
              <% } %>
            <% } %>

            </td>
          <% } %>
          </tr>
        <% } %>
      </table>
      <p><strong>Winner:</strong> <%= game.winner %></p>
      <p><strong>First Played:</strong> <%= game.createdAt %></p>
      <p><strong>Most Recently Played:</strong> <%= game.updatedAt %></p>
    <% } else { %>
      <h1>Game not found.</h1>
      <p>The game with that ID does not exist.</p>
    <% } %>
  </main>

  <footer>
  </footer>

</body>
</html>